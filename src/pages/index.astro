---
import Layout from "../layouts/Layout.astro";
---

<Layout title="Nero Caffe | Prémium Kávézó">
	<nav class="nav">
		<div class="container nav-content">
			<div class="logo-container">
				<img src="/logo.png" alt="Nero Caffe" class="logo-img" />
			</div>
			<div class="nav-links">
				<a href="#about" class="nav-item">Rólunk</a>
				<a href="#menu" class="nav-item">Kínálat</a>
				<a href="#interior" class="nav-item">Helyszín</a>
				<a href="#contact" class="btn btn-outline btn-small">Foglalás</a
				>
			</div>
			<button class="mobile-menu-btn" aria-label="Menü">
				<span></span>
				<span></span>
			</button>
		</div>
	</nav>

	<main>
		<!-- Hero Section -->
		<section class="hero" id="home">
			<div class="hero-bg">
				<img src="/hero.png" alt="Kávé" class="hero-image" />
				<div class="hero-overlay"></div>
			</div>
			<div class="container hero-content">
				<span class="eyebrow fade-in">2015 óta</span>
				<h1 class="hero-title fade-in">
					Ahol a kávé<br /><span class="text-gold text-serif"
						>Művészetté válik</span
					>
				</h1>
				<p class="hero-desc fade-in delay-1">
					Fedezze fel a Nero Caffe világát, ahol a minőségi kávé és a
					nyugodt hangulat találkozik.
				</p>
				<div class="hero-cta fade-in delay-2">
					<a href="#menu" class="btn btn-primary">Étlapunk</a>
					<a href="#about" class="btn btn-outline">Történetünk</a>
				</div>
			</div>
		</section>

		<!-- Rólunk Section -->
		<section id="about" class="about">
			<div class="glow-accent" style="top: 10%; right: -5%;"></div>
			<div class="container split-layout">
				<div class="split-image scroll-reveal">
					<div class="image-frame">
						<img src="/beans.png" alt="Pörkölt kávészemek" />
						<div class="frame-border"></div>
					</div>
				</div>
				<div class="split-content scroll-reveal">
					<span class="eyebrow">Rólunk</span>
					<h2 class="section-title">
						A <span class="text-serif">Tökéletes</span> Csésze Titka
					</h2>
					<p class="text-large">
						A Nero Caffe 2015-ben nyitotta meg kapuit azzal a
						céllal, hogy a város legjobb kávéját kínálja. Szemeinket
						a világ legjobb ültetvényeiről válogatjuk, és helyben
						pörkölve biztosítjuk a frissességet.
					</p>
					<div class="features">
						<div class="feature-item">
							<span class="feature-icon">☕</span>
							<div>
								<h3>Prémium alapanyagok</h3>
								<p>
									100% Arabica különlegességek a világ minden
									tájáról.
								</p>
							</div>
						</div>
						<div class="feature-item">
							<span class="feature-icon">🔥</span>
							<div>
								<h3>Házi pörkölés</h3>
								<p>
									Minden tétel helyben készül, a maximális
									frissesség érdekében.
								</p>
							</div>
						</div>
						<div class="feature-item">
							<span class="feature-icon">✨</span>
							<div>
								<h3>Szenvedély</h3>
								<p>
									Baristáink szeretettel készítik el minden
									egyes kávét.
								</p>
							</div>
						</div>
					</div>
				</div>
			</div>
		</section>

		<!-- Kínálat Section -->
		<section id="menu" class="menu">
			<div class="container">
				<div class="section-header text-center">
					<span class="eyebrow">Kínálat</span>
					<h2 class="section-title">
						Kedvenceink a <span class="text-gold">menüből</span>
					</h2>
				</div>
				<div class="menu-grid">
					<div class="menu-card glass scroll-reveal">
						<h3>Signature Espresso</h3>
						<p>Sötét pörkölésű, csokoládés ízjegyekkel</p>
						<span class="price">890 Ft</span>
					</div>
					<div class="menu-card glass highlight scroll-reveal">
						<span class="badge">Kedvenc</span>
						<h3>Arany Latte</h3>
						<p>
							Krémes tejhab, fahéjjal és egy csipet aranypor
							díszítéssel
						</p>
						<span class="price">1 450 Ft</span>
					</div>
					<div class="menu-card glass scroll-reveal">
						<h3>Hideg Főzésű Kávé</h3>
						<p>12 órán át áztatott, selyemesen lágy ízvilág</p>
						<span class="price">1 290 Ft</span>
					</div>
					<div class="menu-card glass scroll-reveal">
						<h3>Házi Croissant</h3>
						<p>
							Vajas, ropogós tészta, frissen sütve minden reggel
						</p>
						<span class="price">890 Ft</span>
					</div>
					<div class="menu-card glass scroll-reveal">
						<h3>Cappuccino</h3>
						<p>Klasszikus olasz recept, dús tejhabbal</p>
						<span class="price">990 Ft</span>
					</div>
					<div class="menu-card glass scroll-reveal">
						<h3>Sajttorta</h3>
						<p>Krémes New York stílusú desszert, házi készítés</p>
						<span class="price">1 150 Ft</span>
					</div>
				</div>
			</div>
		</section>

		<!-- Helyszín Section -->
		<section id="interior" class="interior">
			<div class="glow-accent" style="bottom: 10%; left: -5%;"></div>
			<div class="container interior-layout">
				<div class="interior-text scroll-reveal">
					<span class="eyebrow">Helyszín</span>
					<h2 class="section-title">
						Egy <span class="text-serif">Nyugodt</span> Menedék
					</h2>
					<p>
						Belső terünket úgy alakítottuk ki, hogy a mindennapi
						rohanásból kilépve valódi kikapcsolódást nyújtson.
						Modern elegancia, kényelmes ülőhelyek, és persze – a
						legjobb kávé illata.
					</p>
					<ul class="interior-features">
						<li>Ingyenes WiFi</li>
						<li>Laptop-barát asztalok</li>
						<li>Privát sarok üzleti megbeszélésekhez</li>
						<li>Hangulatos terasz</li>
					</ul>
				</div>
				<div class="interior-image scroll-reveal">
					<img src="/interior.png" alt="Nero Caffe belső tér" />
				</div>
			</div>
		</section>

		<!-- Kapcsolat Section -->
		<section id="contact" class="contact">
			<div class="container text-center">
				<div class="contact-box glass scroll-reveal">
					<span class="eyebrow">Kapcsolat</span>
					<h2 class="section-title">
						Látogasson <span class="text-gold">meg minket!</span>
					</h2>
					<div class="contact-info">
						<div class="info-item">
							<h3>📍 Cím</h3>
							<p>1051 Budapest<br />Arany János utca 12.</p>
						</div>
						<div class="info-item">
							<h3>🕒 Nyitvatartás</h3>
							<p>H-P: 07:00 - 20:00<br />Sz-V: 09:00 - 21:00</p>
						</div>
						<div class="info-item">
							<h3>📞 Telefon</h3>
							<p><a href="tel:+3612345678">+36 1 234 5678</a></p>
						</div>
					</div>
					<a href="tel:+3612345678" class="btn btn-primary"
						>Asztalfoglalás</a
					>
				</div>
			</div>
		</section>
	</main>

	<footer class="footer">
		<div class="container footer-content">
			<div class="footer-brand">
				<img src="/logo.png" alt="Nero Caffe" class="footer-logo" />
				<p>A város prémium kávézója.</p>
			</div>
			<div class="footer-links">
				<a href="#">Instagram</a>
				<a href="#">Facebook</a>
			</div>
		</div>
		<div class="footer-bottom">
			<div class="container">
				<span>&copy; 2024 Nero Caffe</span>
				<span>Weboldal: Prometheus Digital</span>
			</div>
		</div>
	</footer>
</Layout>

<script>
	// Scroll Reveal Animation
	const observerOptions = { threshold: 0.1 };
	const observer = new IntersectionObserver((entries) => {
		entries.forEach((entry) => {
			if (entry.isIntersecting) {
				entry.target.classList.add("active");
			}
		});
	}, observerOptions);

	document
		.querySelectorAll(".scroll-reveal")
		.forEach((el) => observer.observe(el));

	// Nav Scroll Effect
	window.addEventListener("scroll", () => {
		const nav = document.querySelector(".nav");
		if (window.scrollY > 80) {
			nav.classList.add("scrolled");
		} else {
			nav.classList.remove("scrolled");
		}
	});

	// Mobile Menu Toggle
	const mobileBtn = document.querySelector(".mobile-menu-btn");
	const navLinks = document.querySelector(".nav-links");
	mobileBtn?.addEventListener("click", () => {
		navLinks?.classList.toggle("active");
		mobileBtn.classList.toggle("active");
	});
</script>

<style>
	/* Nav */
	.nav {
		position: fixed;
		top: 0;
		width: 100%;
		z-index: 1000;
		padding: 1.5rem 0;
		transition: all 0.4s ease;
	}
	.nav.scrolled {
		padding: 1rem 0;
		background: rgba(5, 5, 5, 0.95);
		backdrop-filter: blur(15px);
		border-bottom: 1px solid var(--glass-border);
	}
	.nav-content {
		display: flex;
		justify-content: space-between;
		align-items: center;
	}
	.logo-img {
		height: 40px;
	}
	.nav-links {
		display: flex;
		align-items: center;
		gap: 2.5rem;
	}
	.nav-item {
		font-size: 0.85rem;
		font-weight: 500;
		letter-spacing: 0.1em;
	}
	.nav-item:hover {
		color: var(--primary);
	}
	.btn-small {
		padding: 0.7rem 1.5rem;
		font-size: 0.7rem;
	}

	.mobile-menu-btn {
		display: none;
		flex-direction: column;
		gap: 6px;
		background: none;
		border: none;
		cursor: pointer;
		padding: 5px;
	}
	.mobile-menu-btn span {
		width: 25px;
		height: 2px;
		background: var(--text);
		transition: all 0.3s ease;
	}

	/* Hero */
	.hero {
		height: 100vh;
		min-height: 600px;
		position: relative;
		display: flex;
		align-items: center;
		overflow: hidden;
		padding: 0;
	}
	.hero-bg {
		position: absolute;
		top: 0;
		left: 0;
		width: 100%;
		height: 100%;
		z-index: -1;
	}
	.hero-image {
		width: 100%;
		height: 100%;
		object-fit: cover;
	}
	.hero-overlay {
		position: absolute;
		top: 0;
		left: 0;
		width: 100%;
		height: 100%;
		background: linear-gradient(
			90deg,
			rgba(5, 5, 5, 0.85) 0%,
			rgba(5, 5, 5, 0.3) 100%
		);
	}
	.hero-content {
		max-width: 600px;
	}
	.eyebrow {
		display: block;
		font-size: 0.75rem;
		letter-spacing: 0.3em;
		color: var(--primary);
		margin-bottom: 1rem;
		font-weight: 600;
		text-transform: uppercase;
	}
	.hero-title {
		font-size: clamp(2.5rem, 6vw, 4rem);
		line-height: 1.1;
		margin-bottom: 1.5rem;
	}
	.hero-desc {
		font-size: 1.1rem;
		color: var(--text-muted);
		margin-bottom: 2.5rem;
	}
	.hero-cta {
		display: flex;
		gap: 1rem;
		flex-wrap: wrap;
	}

	/* About / Rólunk */
	.split-layout {
		display: grid;
		grid-template-columns: 1fr 1.2fr;
		gap: 5rem;
		align-items: center;
	}
	.image-frame {
		position: relative;
	}
	.image-frame img {
		border-radius: 4px;
	}
	.frame-border {
		position: absolute;
		top: -15px;
		left: -15px;
		width: 60%;
		height: 60%;
		border-top: 2px solid var(--primary);
		border-left: 2px solid var(--primary);
		z-index: -1;
	}
	.section-title {
		font-size: clamp(2rem, 4vw, 2.75rem);
		margin-bottom: 1.5rem;
		line-height: 1.2;
	}
	.text-large {
		font-size: 1.05rem;
		color: var(--text-muted);
		margin-bottom: 2.5rem;
	}
	.features {
		display: flex;
		flex-direction: column;
		gap: 1.5rem;
	}
	.feature-item {
		display: flex;
		gap: 1rem;
		align-items: flex-start;
	}
	.feature-icon {
		font-size: 1.5rem;
	}
	.feature-item h3 {
		font-size: 1rem;
		margin-bottom: 0.25rem;
		font-family: "Montserrat", sans-serif;
		font-weight: 600;
	}
	.feature-item p {
		font-size: 0.9rem;
		color: var(--text-muted);
	}

	/* Menu / Kínálat */
	.menu {
		background: linear-gradient(
			180deg,
			var(--bg) 0%,
			#0a0a0a 50%,
			var(--bg) 100%
		);
	}
	.section-header {
		margin-bottom: 3rem;
	}
	.menu-grid {
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		gap: 1.5rem;
	}
	.menu-card {
		padding: 2rem;
		border-radius: 8px;
		transition: all 0.4s ease;
		position: relative;
	}
	.menu-card:hover {
		transform: translateY(-5px);
		border-color: var(--primary);
	}
	.menu-card h3 {
		font-size: 1.25rem;
		margin-bottom: 0.5rem;
		font-family: "Montserrat", sans-serif;
		font-weight: 600;
	}
	.menu-card p {
		font-size: 0.9rem;
		color: var(--text-muted);
		margin-bottom: 1rem;
	}
	.price {
		font-family: "Cormorant Garamond", serif;
		font-size: 1.25rem;
		color: var(--primary);
		font-weight: 600;
	}
	.highlight {
		border-color: var(--primary);
	}
	.badge {
		position: absolute;
		top: 1rem;
		right: 1rem;
		background: var(--primary);
		color: var(--secondary);
		padding: 0.25rem 0.75rem;
		font-size: 0.65rem;
		font-weight: 600;
		text-transform: uppercase;
		letter-spacing: 0.1em;
		border-radius: 2px;
	}

	/* Interior / Helyszín */
	.interior-layout {
		display: grid;
		grid-template-columns: 1fr 1.5fr;
		gap: 4rem;
		align-items: center;
	}
	.interior-text p {
		color: var(--text-muted);
		margin-bottom: 2rem;
	}
	.interior-features {
		list-style: none;
		display: flex;
		flex-direction: column;
		gap: 0.75rem;
	}
	.interior-features li {
		font-size: 0.9rem;
		color: var(--text-muted);
		padding-left: 1.5rem;
		position: relative;
	}
	.interior-features li::before {
		content: "✓";
		position: absolute;
		left: 0;
		color: var(--primary);
	}
	.interior-image img {
		border-radius: 8px;
	}

	/* Contact / Kapcsolat */
	.contact-box {
		padding: 4rem 3rem;
		max-width: 900px;
		margin: 0 auto;
		border-radius: 8px;
	}
	.contact-info {
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		gap: 2rem;
		margin: 3rem 0;
	}
	.info-item h3 {
		font-size: 1rem;
		margin-bottom: 0.5rem;
		font-family: "Montserrat";
		font-weight: 600;
	}
	.info-item p {
		color: var(--text-muted);
		font-size: 0.95rem;
	}
	.info-item a {
		color: var(--primary);
	}

	/* Footer */
	.footer {
		padding: 4rem 0 2rem;
		background: #000;
		border-top: 1px solid var(--glass-border);
	}
	.footer-content {
		display: flex;
		justify-content: space-between;
		align-items: center;
		margin-bottom: 3rem;
	}
	.footer-logo {
		height: 30px;
		margin-bottom: 1rem;
		opacity: 0.6;
	}
	.footer-brand p {
		color: var(--text-muted);
		font-size: 0.85rem;
	}
	.footer-links {
		display: flex;
		gap: 2rem;
	}
	.footer-links a {
		font-size: 0.85rem;
		color: var(--text-muted);
	}
	.footer-links a:hover {
		color: var(--primary);
	}
	.footer-bottom {
		border-top: 1px solid var(--glass-border);
		padding-top: 2rem;
		font-size: 0.75rem;
		color: var(--text-muted);
	}
	.footer-bottom .container {
		display: flex;
		justify-content: space-between;
	}

	/* Animations */
	.scroll-reveal {
		opacity: 0;
		transform: translateY(40px);
		transition: all 0.8s ease;
	}
	.scroll-reveal.active {
		opacity: 1;
		transform: translateY(0);
	}
	.fade-in {
		opacity: 0;
		animation: fadeIn 1s forwards ease;
	}
	@keyframes fadeIn {
		to {
			opacity: 1;
		}
	}
	.delay-1 {
		animation-delay: 0.3s;
	}
	.delay-2 {
		animation-delay: 0.6s;
	}

	/* Mobile */
	@media (max-width: 992px) {
		.split-layout,
		.interior-layout {
			grid-template-columns: 1fr;
			gap: 3rem;
		}
		.split-image {
			order: 2;
		}
		.menu-grid {
			grid-template-columns: repeat(2, 1fr);
		}
		.contact-info {
			grid-template-columns: 1fr;
			gap: 1.5rem;
		}
	}
	@media (max-width: 768px) {
		.nav-links {
			position: fixed;
			top: 0;
			left: 0;
			width: 100%;
			height: 100vh;
			background: var(--bg);
			flex-direction: column;
			justify-content: center;
			gap: 2rem;
			opacity: 0;
			pointer-events: none;
			transition: opacity 0.3s ease;
		}
		.nav-links.active {
			opacity: 1;
			pointer-events: all;
		}
		.mobile-menu-btn {
			display: flex;
			z-index: 1001;
		}
		.mobile-menu-btn.active span:first-child {
			transform: rotate(45deg) translate(6px, 6px);
		}
		.mobile-menu-btn.active span:last-child {
			transform: rotate(-45deg);
		}
		.menu-grid {
			grid-template-columns: 1fr;
		}
		.footer-content {
			flex-direction: column;
			text-align: center;
			gap: 2rem;
		}
		.contact-box {
			padding: 2.5rem 1.5rem;
		}
	}
</style>
